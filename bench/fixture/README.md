# Inventory

A small inventory library. `src/inventory.js` keeps items and their stock in a `Map` from item name to count. Tests use `node:test`; run them with `npm test`.

New features go in their own folders, each with its code in `index.js` and its tests in `index.test.js`: restocking in `restock/`, reports in `report/`.
